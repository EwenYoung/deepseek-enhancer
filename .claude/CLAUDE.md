# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# network-proxy
- **network-proxy** (`.claude/skills/network-proxy/SKILL.md`) - configure proxy 127.0.0.1:7897 before network commands. Trigger: `/network-proxy`

# dev-workflow
- **dev-workflow** (`.claude/skills/dev-workflow/SKILL.md`) - testing split and Chrome deploy. Trigger: `/dev-workflow`
