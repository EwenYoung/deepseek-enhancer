## 1. CSS 变量与主题系统

- [x] 1.1 在 `src/core/ui-panel.ts` 中注入 `<style id="ds-panel-vars">`，定义浅色/深色 CSS 变量
- [x] 1.2 实现 MutationObserver 监听 `body.dark` 自动切换 `.dark` class
- [x] 1.3 在 `createPanel()` 中初始检测深色模式并应用

## 2. 玻璃面板骨架

- [x] 2.1 重建面板布局：`style.cssText` 引用 CSS 变量，340px 宽，右侧悬浮 12px，圆角 16px
- [x] 2.2 面板滑入/隐藏动画 0.25s ease-out
- [x] 2.3 标题栏：文字 + 关闭按钮
- [x] 2.4 Agent 模式区域：SVG 图标 + 文字 + iOS toggle 开关
- [x] 2.5 Tools 列表：1 列竖向排列，Lucide SVG + 名称 + 标签
- [x] 2.6 导出按钮：MD / HTML 两个按钮，玻璃风格
- [x] 2.7 Skills 列表区：flex:1 可滚动，保留开关、编辑和删除按钮
- [x] 2.8 设置区分页：底部固定，增强功能 / API 设置两个 tab

## 3. 右下角发光触发器

- [x] 3.1 创建右下角固定圆点 `✦`，蓝色发光 + Agent 模式变色
- [x] 3.2 hover 扩展显示「DeepSeek Enhancer」文字标签（0.2s 动画）
- [x] 3.3 点击展开面板，移除拖拽逻辑

## 4. Modal 弹窗系统

- [x] 4.1 创建遮罩层组件
- [x] 4.2 创建编辑弹窗（680px 宽，玻璃风格，居中显示）
- [x] 4.3 创建确认删除弹窗
- [x] 4.4 Ctrl+Enter 快捷键保存，点击遮罩层关闭

## 5. 图标替换与清理

- [x] 5.1 Tools 全部 5 个图标替换为 Lucide SVG
- [x] 5.2 Agent 图标替换为 Lucide SVG
- [x] 5.3 删除 GitHub 导入按钮和相关代码（`skill-importer.ts` 清理完成）
- [x] 5.4 技能卡片：iOS toggle 开关，编辑和删除按钮

## 6. 集成测试

- [x] 6.1 验证深色/浅色切换时面板和弹窗同步 ✅
- [x] 6.2 验证 Agent 模式开关、API Key 保存/测试、增强功能全部正常 ✅
- [x] 6.3 验证新建/编辑/删除 skill 流程正常 ✅
- [x] 6.4 验证 `prefers-reduced-motion` 正常禁用动画
- [x] 6.5 验证 Skills 列表刷新、导入本地 SKILL.md 正常 ✅

## 7. 修复：用户测试发现的 Bug

- [x] 7.1 设置面板不能收起，始终展开遮挡 Skills 列表 ✅
- [x] 7.2 设置面板内部内容仍使用 emoji，与主面板玻璃风格不一致 ✅
- [x] 7.3 设置面板中 toggle 开关按钮的圆点始终卡在左侧，不随状态切换 ✅
- [x] 7.4 Agent 模式前面的 SVG 图标显示为锁而非机器人图标 ✅
- [x] 7.5 导出按钮 MD/HTML 颜色不一致，应统一为浅色背景 ✅
- [x] 7.6 本地导入按钮的黄色文件夹图标改为蓝色，与主题色统一 ✅
- [x] 7.7 Skills 列表表头不固定，滚动列表时表头（含导入/新建按钮）消失
- [x] 7.8 删除确认弹窗和编辑弹窗在深色主题下背景仍为白色，需适配 CSS 变量 ✅
- [x] 7.9 编辑弹窗中"名称"提示文字 `(kebab-case)` 改为普通用户友好的例子 ✅

## 8. 新增功能：面板透明度滑杆

- [x] 8.1 在设置「增强功能」页底部添加透明度滑杆 UI（标签 + 滑杆 + 百分比）
- [x] 8.2 浅色/深色模式各自独立存储透明度值到 `chrome.storage.local`
- [x] 8.3 面板和弹窗的 `var(--panel-bg)` alpha 通道跟随滑杆实时变化
- [x] 8.4 切换深色/浅色模式时自动切换对应的透明度值

## 9. 新增功能：弹窗拖拽 + 拉伸

- [x] 9.1 编辑弹窗标题栏增加拖拽移动功能（鼠标事件）
- [x] 9.2 编辑弹窗四角增加拉伸功能（调整窗口大小）
- [x] 9.3 删除确认弹窗不支持拖拽拉伸
- [x] 9.4 拖拽拉伸仅本次会话有效，不持久化

## 10. 新增功能：Tools 开关

- [x] 10.1 Tools 每行右侧增加小型 iOS toggle 开关，关闭时灰显
- [x] 10.2 工具启用状态存储在 `chrome.storage.local` 的 `ds_mini_tools_state`
- [x] 10.3 通过 postMessage 传递禁用工具列表到 MAIN world
- [x] 10.4 修改 `main-xhr-inject.ts` 的 `TOOL_DEFS` 过滤已禁用的工具

## 11. 第二轮测试发现的问题

- [x] 11.1 Agent 图标看起来仍像锁，需换为更清晰的 AI 图标
- [x] 11.2 导出会话缺少功能名称标签
- [x] 11.3 MD/HTML 按钮点击后无深色高亮变化
- [x] 11.4 设置面板默认收起 + 箭头方向
- [x] 11.5 功能名称模糊：滚动条→隐藏滚动条，自动隐藏→隐藏输入框
- [x] 11.6 API 状态红绿色提示风格不统一
- [x] 11.7 各处 emoji 替换为纯文字（API 测试按钮、skill 按钮、toast）
- [x] 11.8 Prompt 弹出框背景适配深色主题
- [x] 11.9 面板标题"✦ DeepSeek Enhancer"文字颜色为主题色

## 12. 待执行（根据优先级排序）

- [x] 7.7 Skills 列表表头固定（sticky）
- [x] 5.3 清理 skill-importer.ts 的 GitHub 导入代码
- [x] 8.1-8.4 面板透明度滑杆
- [x] 9.1-9.4 弹窗拖拽 + 拉伸
- [x] 10.1-10.4 Tools 开关
